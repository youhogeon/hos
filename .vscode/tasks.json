{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "make",
      "type": "shell",
      "command": "HOS_VERSION=$(date +dev%y%m%d.%S) make clean disk.img",
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "options": {
        "cwd": "${workspaceFolder}",
      },
      "problemMatcher": []
    },
    {
      "label": "run qemu",
      "type": "shell",
      "dependsOn": "make",
      "command": "[ ! -f HDD.img ] && qemu-img create HDD.img 20M; qemu-system-x86_64 -L . -m 64 -fda ./build/disk.img -hda ./HDD.img -boot a -display vnc=0.0.0.0:0 -audiodev none,id=none -monitor stdio",
      "options": {
        "cwd": "${workspaceFolder}",
      }
    }
  ]
}
